'use strict';
var fs = require("fs");

// Development specific configuration
// ==================================
module.exports = {
    // MongoDB connection options
    // uncomment below configuration for mongodb configuration 
    // mongo: { uri: 'mongodb://username:password@dbhost:port/dbname' },
    seedDB: true
}